{
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "home",
    "reader"
  ],
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "concurrently": "^9.0.1",
    "pdfjs-dist": "^4.7.76",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-pdf": "^9.1.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start:home": "npm start --workspace=home",
    "start:reader": "npm start --workspace=reader",
    "build:home": "npm run build --workspace=home",
    "build:reader": "npm run build --workspace=reader",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:all": "concurrently \"npm start --workspace=home\" \"npm start --workspace=reader\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
